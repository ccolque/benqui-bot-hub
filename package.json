{
  "name": "benqui-bot-hub",
  "version": "0.1.0",
  "private": true,
  "description": "API multi-tenant para bots de WhatsApp (Meta Cloud API) desplegada en Vercel",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vercel/functions": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
